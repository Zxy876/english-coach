# Copy this file to .env.local and fill in your key.
# .env.local is gitignored; .env.example is safe to commit.

GEMINI_API_KEY=
LLM_PROVIDER=gemini
# SQLite for local dev; for prod swap to a Postgres URL.
DATABASE_URL="file:./dev.db"
